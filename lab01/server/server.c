#include "server.h"

int main(int argc, char *argv){
  int sock_fd, new_fd;
  pid_t childpid;
  socklen_t clilen;
  struct sockaddr_in cliaddr, servaddr;
  sock_f = socket(PF_INET, SOCK_STREAM, 0);
  
}
